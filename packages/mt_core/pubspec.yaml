name: mt_core
description: "The core: MeTube API client, models, download engine, storage, backup, logging — pure Dart, no Flutter."
version: 0.1.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.13.0

dependencies:
  dio: ^5.7.0
  encrypt: ^5.0.3
  synchronized: ^3.3.0
  uuid: ^4.5.1

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
