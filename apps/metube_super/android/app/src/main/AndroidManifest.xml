<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- الشبكة: البث والسحب من سيرفر المالك (يضيفها Flutter في debug فقط). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- التشغيل الخلفي وإشعار الوسائط (م-21) + إبقاء الشاشة أثناء الفيديو. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- **خلل مصطاد على جهاز المالك (2026-09-01):** كان هذا التصريح
         ناقصاً كلياً في Super، فأندرويد 13+ يمنع إشعار الوسائط بصمت —
         الصوت يعمل بالخلفية ولا شيء يظهر في ستارة الإشعارات ولا على
         شاشة القفل. (فحص `dumpsys package`: Lite يملكه وSuper لا.) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- فخ §6.2: Dio يتجاوز قيد cleartext وExoPlayer لا — سيرفر المالك
         محلي بـ http، لذلك Super يسمح cleartext عاماً (Lite يقيّده
         بالنطاقات الخاصة في المرحلة 7). بدونه: التحميل يعمل والبث يفشل. -->
    <application
        android:label="MeTube Super"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- م-3: بدون هذه المرشحات لا يظهر التطبيق في ورقة مشاركة
                 أندرويد إطلاقاً — مانيفست `receive_sharing_intent` فارغ
                 ولا يحقنها (خلل مصطاد في المرحلة 7). -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <!-- م-41: اختصارات ضغطة الأيقونة المطولة. **لا مرشح نوايا
                 ولا `data`**: الاختصار الساكن يستدعي المكوّن صراحةً،
                 وأي `data` في النية يختطفها Flutter كمسار إقلاع فينتهي
                 المستخدم في «Page Not Found» (مصطاد على المحاكي). -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity>
        <!-- خدمة الصوت الخلفي ومستقبل أزرار الوسائط (audio_service). -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- م-20 «فتح الرابط الأصلي» عبر url_launcher (رؤية الحزم — أندرويد 11). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
