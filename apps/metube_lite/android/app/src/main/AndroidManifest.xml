<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- الشبكة: إرسال الرابط للسيرفر وسحب الملف (يضيفها Flutter في debug فقط). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- م-10: استمرار التحميل بالخلفية (خدمة أمامية للمزامنة) + م-21 الصوت. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <!-- م-9: إشعارات التقدم والاكتمال والخطأ (33+ تتطلب طلباً صريحاً). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>
    <!-- الوسائط: قراءة ما نزّلناه لعرضه في المكتبة (م-12). -->
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO"/>
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"/>

    <application
        android:label="MeTube Lite"
        android:name="${applicationName}"
        android:networkSecurityConfig="@xml/network_security_config"
        <!-- نسخ أندرويد التلقائي: مفعَّل، لكن **بلا ملف التخزين الآمن**
             (انظر res/xml/backup_rules.xml — عطل كامن كُشف 2026-09-04). -->
        android:allowBackup="true"
        android:fullBackupContent="@xml/backup_rules"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- م-3: استقبال المشاركة من أي تطبيق — رابط واحد أو عدة روابط.
                 `receive_sharing_intent` لا يحقن هذه المرشحات (مانيفست
                 الحزمة فارغ)، فبدونها لا تظهر أيقونة التطبيق في ورقة
                 المشاركة إطلاقاً. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND_MULTIPLE"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <data android:mimeType="text/plain"/>
            </intent-filter>
            <!-- م-41: اختصارات ضغطة الأيقونة المطولة. **لا مرشح نوايا
                 ولا `data`**: الاختصار الساكن يستدعي المكوّن صراحةً،
                 وأي `data` في النية يختطفها Flutter كمسار إقلاع فينتهي
                 المستخدم في «Page Not Found» (مصطاد على المحاكي). -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts"/>
        </activity>
        <!-- م-10: خدمة `flutter_background` الأمامية التي تُبقي التحميل
             حياً بالخلفية. **بدون هذا التصريح** يرفض النظام تشغيلها
             (`Unable to start service … IsolateHolderService: not found`)
             فيبقى وضع الخلفية معطلاً بصمت — خلل مصطاد في المرحلة 8. -->
        <service
            android:name="de.julianassmann.flutter_background.IsolateHolderService"
            android:foregroundServiceType="dataSync"
            android:enabled="true"
            android:exported="false"/>
        <!-- خدمة الصوت الخلفي ومستقبل أزرار الوسائط (audio_service). -->
        <service
            android:name="com.ryanheise.audioservice.AudioService"
            android:foregroundServiceType="mediaPlayback"
            android:exported="true">
            <intent-filter>
                <action android:name="android.media.browse.MediaBrowserService"/>
            </intent-filter>
        </service>
        <receiver
            android:name="com.ryanheise.audioservice.MediaButtonReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON"/>
            </intent-filter>
        </receiver>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- م-20 «فتح الرابط الأصلي» عبر url_launcher (رؤية الحزم — أندرويد 11). -->
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="https"/>
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW"/>
            <data android:scheme="http"/>
        </intent>
    </queries>
</manifest>
