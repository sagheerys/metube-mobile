# ===== Flutter / Dart =====
devtools_options.yaml
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
.pub-cache/
coverage/
*.lock.json

# ===== Android =====
**/android/.gradle/
# مخلَّفات مصرّف Kotlin (جلسات وأخطاء) — تظهر مع Built-in Kotlin في AGP الحديث.
**/android/.kotlin/
**/android/local.properties
**/android/key.properties
*.keystore
*.jks
captures/

# ===== مخرجات لا تدخل المستودع (درس النسخ القديمة: APK 62MB وفيديوهات داخل git!) =====
*.apk
*.aab
*.zip
*.mp4
*.metube_backup*.json
metube_*_backup.json

# ===== أسرار =====
.env
.env.*
*.secret

# ===== IDE / OS =====
.idea/
.vscode/*
!.vscode/extensions.json
*.iml
.DS_Store
Thumbs.db
desktop.ini

# ===== سجلات ومؤقتات =====
*.log
logs/
tmp/

# مفاتيح التوقيع — لا تدخل git أبداً (نسخها في نسخة احتياطية أولى لمفاتيح التوقيع و نسخة احتياطية ثانية لمفاتيح التوقيع)
keys/

# مفاتيح النسخ الاحتياطية — لا تدخل git أبداً (المفتاح + النسخة = بيانات مكشوفة)
*_backup_key.txt

# ===== مهارات الوكيل المستوردة =====
# محتوى طرف ثالث (مصادره وبصماته في skills-lock.json) — لا يُعاد نشره من
# هنا. استعِدها من مصادرها عند الحاجة.
.claude/skills/

# ── Local-only, archived out of the public repo (2026-09-09) ──
# These stay on the maintainer's disk and in the maintainer backup drive; they are not
# useful to anyone cloning the project and some carry local paths.
CLAUDE.md
RELEASE_NOTES.md
backup.cmd
checkpoint.cmd
tool/auto-checkpoint.ps1
tool/backup.ps1
tool/build-apk.ps1
tool/checkpoint.ps1
tool/restore.ps1
tool/build-lite.log
tool/cmdtools-url.txt
tool/flutter-url.txt
.design/
docs/reports/
docs/proposals/
docs/plan/_archive/
docs/TEST-MATRIX.md
